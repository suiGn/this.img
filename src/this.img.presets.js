// Define the preset configurations
const PRESETS = {
  default64: { width: 64, height: 64, channels: 3, format: 'jpeg' },
  small32: { width: 32, height: 32, channels: 3, format: 'jpeg' },
  large128: { width: 128, height: 128, channels: 3, format: 'jpeg' },
  grayscale64: { width: 64, height: 64, channels: 1, format: 'jpeg' },
  grayscale32: { width: 32, height: 32, channels: 1, format: 'jpeg' },
  highResColor256: { width: 256, height: 256, channels: 3, format: 'jpeg' },
  highResGray256: { width: 256, height: 256, channels: 1, format: 'jpeg' }
};

  export default PRESETS;
  